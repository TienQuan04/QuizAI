<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Internet cho Gemini API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizzAI"
        tools:targetApi="31">
        <activity
            android:name=".DetailActivity"
            android:exported="false" />
        <!-- ================= LOGIN (LAUNCHER) ================= -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- ================= REGISTER ================= -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- ================= MAIN (HOME) ================= -->
        <activity
            android:name=".MainActivity"
            android:exported="true" /> <!-- ================= LOADING ================= -->
        <activity
            android:name=".LoadingActivity"
            android:exported="false" /> <!-- ================= QUIZ ================= -->
        <activity
            android:name=".QuizActivity"
            android:exported="false" /> <!-- ================= RESULT ================= -->
        <activity
            android:name=".ResultActivity"
            android:exported="false" /> <!-- ================= HISTORY ================= -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false" /> <!-- ================= PROFILE ================= -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
    </application>

</manifest>